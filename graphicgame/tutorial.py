#!/usr/bin/env python3

import sys
import pygame

pygame.init()
size = width, height = 16, 9
speed = [2, 2]
black = 0, 0, 0

screen = pygame.display.set_mode((size), pygame. FULLSCREEN)

input("wait")

